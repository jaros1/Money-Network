angular.module('MoneyNetwork')

    .controller('WalletCtrl', ['$window', function ($window) {
        var self = this;
        var controller = 'WalletCtrl';
        console.log(controller + ' loaded');

        // end WalletCtrl
    }])

;